name: Build macOS App

on:
  workflow_dispatch:

jobs:
  build-macos-app:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Select Xcode
        run: |
          set -euo pipefail
          for candidate in /Applications/Xcode_26.1.1.app /Applications/Xcode_26.1.app /Applications/Xcode.app; do
            if [[ -d "$candidate" ]]; then
              sudo xcode-select -s "${candidate}/Contents/Developer"
              echo "DEVELOPER_DIR=${candidate}/Contents/Developer" >> "$GITHUB_ENV"
              break
            fi
          done
          xcodebuild -version

      - name: Build macOS app (Release)
        run: swift build -c release --product CodexBar

      - name: Package app bundle
        id: pkg
        shell: bash
        run: |
          set -euo pipefail
          BIN_DIR="$(swift build -c release --product CodexBar --show-bin-path)"
          BUILD_DIR="$(dirname "$BIN_DIR")"

          APP_DIR="${RUNNER_TEMP}/CodexBar.app"
          mkdir -p "$APP_DIR/Contents/MacOS"
          mkdir -p "$APP_DIR/Contents/Frameworks"
          mkdir -p "$APP_DIR/Contents/Resources"

          cp "$BIN_DIR/CodexBar" "$APP_DIR/Contents/MacOS/CodexBar"

          # Copy Sparkle.framework
          SPARKLE_SRC="$(find "$BUILD_DIR" -path "*/Sparkle.framework" -type d 2>/dev/null | head -1)"
          if [[ -z "$SPARKLE_SRC" ]]; then
            SPARKLE_SRC="$(find .build -path "*/Sparkle.framework" -type d 2>/dev/null | head -1)"
          fi
          if [[ -n "$SPARKLE_SRC" ]]; then
            echo "Found Sparkle at: $SPARKLE_SRC"
            cp -R "$SPARKLE_SRC" "$APP_DIR/Contents/Frameworks/Sparkle.framework"
          else
            echo "WARNING: Sparkle.framework not found"
          fi

          # Copy any dylibs
          for dylib in "$BIN_DIR"/*.dylib; do
            [[ -f "$dylib" ]] && cp "$dylib" "$APP_DIR/Contents/MacOS/"
          done

          # Fix rpath
          install_name_tool -add_rpath @executable_path/../Frameworks "$APP_DIR/Contents/MacOS/CodexBar" 2>/dev/null || true

          cat > "$APP_DIR/Contents/Info.plist" << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>CodexBar</string>
              <key>CFBundleIdentifier</key>
              <string>com.steipete.codexbar</string>
              <key>CFBundleName</key>
              <string>CodexBar</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>0.20.0</string>
              <key>LSUIElement</key>
              <true/>
              <key>LSMinimumSystemVersion</key>
              <string>14.0</string>
          </dict>
          </plist>
          EOF

          echo "app_dir=$APP_DIR" >> "$GITHUB_OUTPUT"
          echo "Contents of app bundle:"
          find "$APP_DIR" -type f | head -20

      - name: Upload macOS app
        uses: actions/upload-artifact@v6
        with:
          name: CodexBar-macOS
          path: ${{ steps.pkg.outputs.app_dir }}
